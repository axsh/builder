---
networks:
  aws:
    network_type: "aws"
    ipv4_network: "192.168.100.0"
    prefix: 24
    subnet_id:
nodes:
  dcmgr:
    provision: 
      spec:
        type: "aws"
        image_id: "ami-xxxxxx"
        instance_type: "t2.micro"
        key_pair: 'keypair'
        nics:
          eth0:
            network: "aws"
      provisioner: "shell"
      data:
        - "/path/to/provision_script"
    ssh:
      user: "ec2-user"
      key: "/path/to/private_key"
